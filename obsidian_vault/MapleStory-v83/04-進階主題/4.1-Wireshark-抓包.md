# 4.1 Wireshark 抓包教學

## 概述
使用 Wireshark 抓取 MapleStory 網路封包，分析客戶端與伺服器之間的通訊。

## Wireshark 下載
官網：https://www.wireshark.org/

## 抓包步驟

### 1. 設置過濾器
MapleStory 使用的連接埠通常為：
- 登入端口：7575
- 遊戲端口：8484 (或自定義)

### 2. 過濾表達式
```
tcp.port == 7575 || tcp.port == 8484
```

### 3. 分析重點
- **封包方向**：客戶端發送 (C→S) vs 伺服器發送 (S→C)
- **封包大小**：識別不同類型的操作
- **封包內容**：尋找可讀字串（Hello Packet 為純文字）

## 🐾 v83 封包加密架構

### 三層加密結構
```
┌─────────────────────────────────────────────────────┐
│  1. MapleCrypto (頭部混淆)                          │
│     - 防止靜態分析 opcode                           │
│     - Header: 4 bytes (長度 + 混淆 opcode)           │
│                                                        │
│  2. Shanda Encryption (對稱加密)                     │
│     - 早期版本使用                                   │
│     - 已於 v118+ 移除                               │
│                                                        │
│  3. AES-OFB (主加密)                                │
│     - v83 主要加密方式                              │
│     - 256-bit key, 4-byte IV                       │
└─────────────────────────────────────────────────────┘
```

### 重要原則
> **第一個發送/接收的封包永遠是純文字（未加密）**

這個未加密的封包就是 **Hello Packet**，用於協商加密參數。

## 📨 Hello Packet 詳細結構

### 客戶端 → 伺服器 (C→S) Hello
```
偏移    長度   內容
0x00    2      封包總長度 (little endian)
0x02    2      Maple 版本號 (如 0x0053 = 83)
0x04    n      版本字串 (如 "83")
n+4     4      SendIV (客戶端指定的發送 IV)
n+8     4      RecvIV (客戶端指定的接收 IV)
n+12    2      地區標誌 (MAPLE_LOCALE)
n+14    2      Unknown (未知用途)
```

### 伺服器 → 客戶端 (S→C) Hello
```
偏移    長度   內容
0x00    2      封包總長度
0x02    2      Maple 版本號
0x04    n      版本字串
n+4     4      RecvIV (伺服器回應)
n+8     4      SendIV (伺服器回應)
n+12    2      地區標誌
n+14    2      Unknown
```

### MapleCrypto 頭部混淆演算法
```python
# 加密端：對 opcode 進行混淆
def maple_encrypt_header(opcode, iv):
    verify = (opcode ^ iv[0]) + (iv[1] << 8)
    verify = (verify << 3) | (verify >> 5)  # 位旋轉 3 位
    return verify & 0xFFFF

# 解密端：反向操作
def maple_decrypt_header(encrypted, iv):
    verify = encrypted
    verify = (verify >> 3) | ((verify << 5) & 0xFFFF)  # 反向旋轉
    return (verify - (iv[1] << 8)) ^ iv[0]
```

## 📡 登入流程封包序列

### 完整登入流程
```
1. C→S: Hello Packet (純文字)
   → 客戶端告知版本、IV

2. S→C: Hello Packet (純文字)
   → 伺服器確認版本、回應 IV

3. C→S: 登入請求 (已加密)
   Opcode: 0x01 (LOGIN_PASSWORD)
   Data: 帳號 + 密碼 (已 AES 加密)

4. S→C: 登入結果 (已加密)
   Opcode: 0x00 (LOGIN_STATUS)
   Data: 結果碼 + 帳號資訊

5. C→S: 世界選擇 (已加密)
   Opcode: 0x05 (SELECT_CHARLIST)

6. S→C: 角色列表 (已加密)
   Opcode: 0x03 (CHARLIST)
   Data: 角色資料陣列

7. C→S: 選擇角色進入遊戲 (已加密)
   Opcode: 0x0B (PLAYER_DROP)
```

## 🔧 Wireshark 實戰技巧

### 設定顏色規則
```
紅色 (異常):    tcp.len > 2000      (大型封包)
綠色 (客戶端):  ip.src == 127.0.0.1 (本地發送)
藍色 (伺服器):  ip.dst == 127.0.0.1 (本地接收)
黃色 (登入):    tcp.port == 7575    (登入端口)
```

### Follow TCP Stream 分析
1. 右鍵任意封包 → Follow → TCP Stream
2. 檢視完整會話的明文部分（Hello Packet）
3. 分離 C→S 和 S→C 流量
4. 識別加密封包的長度規律

### 識別加密封包
```
徵兆：
- 封包長度固定 (如每次都是 48 bytes)
- 內容看起來像隨機位元組
- 相同操作產生的封包每次都不同 (因為 IV 變化)
```

## 🛠️ 專用抓包工具

### Maple_Pshark (v83 專用)
GitHub: https://github.com/obstriker/Maple_Pshark
- 專為 v83 設計的封包攔截器
- 自動處理 AES 解密
- 適用於 MapleRoyals 等私人伺服器

### MaplePacketPuller (Python)
GitHub: https://github.com/Bratah123/MaplePacketPuller
- Python 撰寫的 MapleStory 封包結構分析器
- 支援多種版本
- 可自動解析封包結構

### MapleRef Wiki - Packet 結構參考
https://mapleref.fandom.com/wiki/Packets
- 完整的封包結構文檔
- 涵蓋各版本 Opcode 對應關係
- 可配合 Wireshark 使用

### NGS Bypass + Packet Hack
- 可繞過 integrity check
- 攔截並修改封包
- 適用於分析客戶端加密邏輯

## ⚠️ 注意事項
- v83 的 AES 金鑰由版本決定，需與客戶端匹配
- Wireshark 無法直接解密 AES，需使用外部工具或客戶端分析
- 第一個封包（Hello Packet）是最關鍵的分析起點

---

## 🆕 專用抓包工具 (2024-2025 更新)

### Maple_Pshark (v83 專用封包 Logger)
GitHub: https://github.com/obstriker/Maple_Pshark
- 專為 MapleRoyals 等私人伺服器設計的封包截獲器
- 自動處理 AES 解密
- 適用於 v83 MapleStory 私人伺服器

### MaplePacketPuller (Python 封包分析器)
GitHub: https://github.com/Bratah123/MaplePacketPuller
- Python 撰寫的 MapleStory 封包結構分析器
- 支援多種版本
- 可自動解析封包結構

### Wireshark 顏色規則建議
| 顏色 | 過濾條件 | 說明 |
|------|---------|------|
| 紅色 | `tcp.len > 2000` | 大型封包（可能是錯誤） |
| 綠色 | `ip.src == 127.0.0.1` | 客戶端發送封包 |
| 藍色 | `ip.dst == 127.0.0.1` | 伺服器接收封包 |
| 黃色 | `tcp.port == 7575` | 登入端口流量 |

### 識別加密封包的徵兆
- 封包長度固定（如每次都是 48 bytes）
- 內容看起來像隨機位元組
- 相同操作產生的封包每次都不同（因為 IV 每次變化）
- 封包頭部有一定規律（可用於識別封包類型）

### MapleRef Wiki - Packet 結構參考
https://mapleref.fandom.com/wiki/Packets
- 完整的封包結構文檔
- 涵蓋各版本Opcode 對應關係

---

*🐱 超级猫咪 - 更新於 2026-03-24 (第三次)*
