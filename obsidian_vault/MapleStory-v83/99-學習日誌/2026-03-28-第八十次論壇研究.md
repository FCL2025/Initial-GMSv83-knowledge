# 2026-03-28 第七十次論壇研究

**時間**: 2026-03-28 08:57 UTC
**重點**: 登入器/客戶端修改、Opcode結構更新、抓包工具整理、NPC腳本進階

---

## 研究重點一：v83 登入器/客戶端修改工具鏈

### Hex Editor + STREDIT 流程（maplesecrets.blogspot.com, 2013）

**原文重點**：
> "I'm not sure if anyone has done this before, but I'll just start it now... Here is a simple tutorial on hex editing clients for maplestory. This has only been tested on v83, but will probably work for any other version."

**完整流程**：
```
1. 使用 OllyDbg 開啟 MapleStory 客戶端 (F3)
2. 使用 Cheat Engine 配合腳本進行修改
3. 按 Ctrl+G 跳到目標位址 (如 0x00932501)
4. 勾選 Hack 並確認 OllyDbg 中的變化
5. 右鍵 → Copy to executable → All modifications
6. 保存修改後的執行檔
```

**IP 位址 Hex 搜尋重點**：
- `127.0.0.1` → Hex: `31 37 32 2E 30 2E 30 2E 31`
- IP 位址中的 `.` → Hex: `2E`
- 若目標 IP 比 `127.0.0.1` 長 → 剩餘空間填 `00`

### STREDIT 工具功能（RaGEZONE, MapleSolaxiaV2 README）

**GitHub**: https://forum.ragezone.com/threads/release-stredit-localhost-editor.810762/

| 版本 | 功能 |
|------|------|
| 2.9.2.0 | 支援 IP 修改 (2012) |
| 最新版 | 修復 v83 加密相容性 |

**STREDIT 標準流程**：
```
1. File → Open → 選擇 localhost.exe
2. 詢問「是否 v118+」→ 選「No」(v83 適用)
3. 詢問「IP 搜尋模式」→ 選「Yes」
4. 點擊「Force to a IP」按鈕
5. 輸入目標 IP
6. 保存
```

---

## 研究重點二：Opcode 結構深度更新

### MapleRef Wiki 核心原則（mapleref.fandom.com）

> "Transmitting from the server and receiving on the client has one set of agreed upon Opcodes, where transmitting from the client and receiving on the server has yet another."

**Opcode 方向對照**：
| 方向 | 說明 |
|------|------|
| **S→C (Send)** | 伺服器發送到客戶端，Server Opcode Set |
| **C→S (Recv)** | 客戶端發送到伺服器，Client Opcode Set |

**重要**：同一個 Opcode 數字在不同方向代表**完全不同**的含義！

### MapleRef Packet 結構總覽

**URL**: https://mapleref.fandom.com/wiki/Packets

**封包頭部格式**：
- **Packet headers are 4 bytes long**
- Generated using: send IV + packet length (without header) + version of MapleStory
- **Version is -(version + 1)** when sending to client, otherwise just plain version
- **Opcode 是 short (2 bytes long)** in v83

### v83 Opcode 演進

> "In earlier versions, the Opcode was the first byte of the packet. However, the Opcode was changed to a short (2 bytes long). This changed the maximum amount of Opcodes potentially available from 255 (2^8) to 65,536 (2^16)."

### MapleCrypto 頭部混淆公式

```java
// 加密端 (getPacketHeader)
int verify = (opcode ^ (iv & 0xFF)) + ((iv >> 8) & 0xFF) * 0x100;
verify = (verify << 3) | (verify >>> 5);  // rotate left 3 bits
verify &= 0xFFFF;
return new byte[] {
    (byte) (verify & 0xFF),
    (byte) ((verify >>> 8) & 0xFF),
    (byte) (length & 0xFF),
    (byte) ((length >>> 8) & 0xFF)
};

// 解密端 (retrieveOpcode)
int verify = (header[0] & 0xFF) | ((header[1] << 8) & 0xFF00);
verify = (verify >>> 3) | ((verify << 5) & 0xFFFF);
return (verify - ((iv >> 8) & 0xFF) * 0x100) ^ (iv & 0xFF);
```

---

## 研究重點三：v83 抓包工具完整整理

### Maple_Pshark（GitHub, obstriker/Maple_Pshark）

**URL**: https://github.com/obstriker/Maple_Pshark

**功能**：
1. Intercepting Recv messages
2. Modify messages
3. Injecting packets for send or recv
4. Rule based packet manipulation/ filtering

> "This is a Maplestory v83 packet logger for private servers. This piece of software hooks to the encryption function and let you intercept messages as they go."

**平台**：Windows (.NET)
**用途**：即時截獲 v83 封包，Hook 加密函數，直接獲取明文

### MaplePacketPuller（GitHub, Bratah123/MaplePacketPuller）

**URL**: https://github.com/Bratah123/MaplePacketPuller

**功能**：
- Reads keywords from IDA-generated pseudocode
- Formats them into server encode-friendly form
- Shows structure of packets
- Writes packet structures to output files

**支援版本**：
| 環境 | 支援狀態 |
|------|---------|
| Python 3.8.5 | ✅ |
| Python 3.6.12 & 3.8.6 | ✅ |
| IDA Pro 7.0 (32-bit) | ✅ |
| IDA Pro 7.5 | ✅ |
| IDA Pro 6.8 | ✅ |
| Python 2.7 | ❌ (os.scandir) |

### MapleShark（GitHub, diamondo25/MapleShark）

**URL**: https://github.com/diamondo25/MapleShark

> "MapleShark is a MapleStory sniffer that works with SharpPcap. Sniff dem packetzz!"

### icelemon1314/mapleSniffer（GitHub）

**URL**: https://github.com/icelemon1314/mapleSniffer

> "maplestory packet capture"

### MaplePE（GitHub, zhyonc/MaplePE）

**URL**: https://github.com/zhyonc/MaplePE

| 功能 | 說明 |
|------|------|
| 封包解析 | Log and format packet structures as list items |
| 自訂封包發送 | Edit packet data and send it to the client |
| 目標進程提取 | Extract InPacket and OutPacket from target process |
| Opcode 過濾 | Filter specific opcodes |
| 程式碼生成 | Generate simple code from structures |

**技術架構**：
- MaplePE.exe → UDP server，接收封包資訊
- Packet.dll → Hook 函數讀寫封包，注入目標進程
- 使用 CreateRemoteThread + LoadLibraryW 注入
- 使用 UDP client 發送封包資訊

**封包發送前提條件**：
> "Before sending a packet via MaplePE.exe, the server must respond with at least one packet to ensure that the CClientSocket instance can be correctly specified."

### 抓包工具比較總表

| 工具 | 類型 | 加密處理 | 封包注入 | IDA 整合 |
|------|------|---------|---------|---------|
| Maple_Pshark | 即時截獲 | Hook，直接明文 | ✅ | ❌ |
| MaplePacketPuller | 離線分析 | 需手動解密 | ❌ | ✅ |
| MapleShark | Wireshark plugin | 需解密 | ❌ | ❌ |
| mapleSniffer | 通用截獲 | 需解密 | ❌ | ❌ |
| MaplePE | 編輯器 | Hook，直接明文 | ✅ | ❌ |

---

## 研究重點四：v83 NPC 腳本編寫進階

### NPC TYPE 值完整對照表（RaGEZONE, 2026-03）

**URL**: https://forum.ragezone.com/threads/npc-type.1262188/

| Type 值 | 名稱 | 客戶端行為 | 腳本需求 | 範例 |
|---------|------|-----------|---------|------|
| `0` | 普通 NPC | 點擊後發送 NPC_TALK 封包 | ✅ 需要 `.js` 腳本 | 一般任務/對話 NPC |
| `1` | 商店 NPC | 點擊後直接開啟商店介面 | ⚠️ 可有可無 | 商人、鐵匠 |
| `2` | 拍賣 NPC | 點擊後開啟拍賣介面 | ❌ 不需要 | 拍賣管理員 |
| `3` | 保險箱 NPC | 點擊後開啟保險箱 | ❌ 不需要 | 銀行 NPC |
| `4~6` | 特殊 NPC | 各類特殊功能 | ❌ 不需要 | 各類系統 NPC |

### NPC 腳本源相容性問題（RaGEZONE, 2026-03）

**問題**：
- 不同音源（Cosmic、Kinoko、HeavenMS）的 NPC 腳本解析器版本不同
- `importPackage` 和 `SavedLocationType` 處理差異
- JDK 7+ 已廢棄 `importPackage`

**解決方案**：
1. 使用普遍支援的 `cm.` 函數
2. 避免使用特定音源專有的 Package 路徑
3. **避免 `importPackage`**（改用 `const Xxx = Java.type("...")`）
4. 先在本地測試再部署

### Dynamic NPC Shop Script（RaGEZONE, 2016）

**URL**: https://forum.ragezone.com/threads/dynamic-npc-shop-script-v83.1089979/

**概念**：使用 JavaScript 動態定義商店，**避免每次修改都要重啟伺服器**

```javascript
// 動態商店 NPC
var items = Array(
    Array(4000000, 1000, 1),  // [物品ID, 價格, 數量]
    Array(4000001, 2000, 5),
    Array(4000002, 500, 10)
);

function start() {
    var shop = cm.openShop(1);
    for (var i = 0; i < items.length; i++) {
        shop.addItem(items[i][0], items[i][1], items[i][2]);
    }
    shop.send();
}
```

### NPC TYPE 在 HaRepacker 中的修改方式

```
1. HaRepacker 開啟 NPC.wz
2. 找到目標 NPC 的 .img 檔案（如 9000000.img）
3. 展開節點：9000000.img → info
4. 找到 type 屬性
5. 雙擊修改值
6. 保存並重新載入 WZ
```

---

## 研究重點五：HeavenMS 完整研究（GitHub README）

### Ronan 專案 vs MapleSolaxiaV2

**GitHub**: https://github.com/ronancpl/HeavenMS

> "Regarding distributability and usage of the code presented here: like it was before, this MapleStory server is open-source."

**與 MapleSolaxiaV2 的關係**：
- HeavenMS 是 MapleSolaxiaV2 的 fork
- 添加了大量伺服器核心改進
- 提供了完整的 NPC 腳本源碼
- 是學習 v83 伺服器開發的最佳開源專案之一

### WZ 重要性說明

> "In this project, many gameplay-wise issues generated from either the original WZ files and the server source have been partially or completely solved. Considering the use of the provided edited WZ's and server-side wz.xml files should be of the greatest importance when dealing with this instance of private server, in order to perceive it at it's full potential."

### WZ 不能直接使用原廠 Nexon WZ

> "This is not a purely v83 emulator (cannot directly use Nexon/Wizet's original WZ files). You must use the provided custom WZ files."

### 客戶端下載（Google Drive）

**URL**: https://drive.google.com/drive/folders/0BzDsHSr-0V4MYVJ0TWIxd05hYUk

包含內容：
- v83 localhost 客戶端
- HaRepacker 工具
- SQL 初始化腳本 (db_drops.sql 等)
- 伺服器設定範例
- 常用腳本範例

### MapleSolaxiaV2 架設重點（GitHub README）

```
1. 安裝 WampServer2.0i.exe (MySQL)
2. 安裝 JDK 8 + NetBeans
3. 建立資料庫：執行 db_database.sql, db_drops.sql
4. optional: db_shopupdate.sql (新增商品到商店)
5. 設定 configuration.ini 中的 IP
6. 用 NetBeans 開啟專案（with existing code）
7. 加入 JAR libraries from cores folder
8. Clean and Build → 產生 JAR
9. 執行 launch.bat 啟動伺服器
```

### 客戶端架設重點

```
1. 使用 ManagerMsv83.exe 安裝 MapleStory
2. 刪除：HShield folder, ASPLnchr.exe, MapleStory.exe, Patcher.exe
3. 用 localhost.exe 替換
4. 用提供的 WZ 覆蓋原廠 WZ
5. 若 IP 不是 127.0.0.1 → 用 Hex Editor 修改 localhost.exe
6. 執行 localhost.exe 進入遊戲
```

---

## 研究重點六：v83 私服生態 2026 年 3 月現況

| 伺服器 | 類型 | 狀態 |
|--------|------|------|
| **KuroMS** | Pre-BB v83 | 無 P2W，持續營運 (playkuro.com) |
| **TimelessMS** | v83 | Beta V2 (2025-10) |
| **MapleLegacy** | v83→v92 | 升級中 |
| **Cosmic** | 開源模擬器 | 活躍更新 (Java 21) |
| **HeavenMS** | 開源模擬器 | 維護中 |
| **RustMS** | Rust 實驗 | 2026-03 重啟 |

---

## 關鍵連結整理

| 資源 | URL |
|------|-----|
| HeavenMS GitHub | https://github.com/ronancpl/HeavenMS |
| MapleSolaxiaV2 | https://github.com/skhzhang/MapleSolaxiaV2 |
| Maple83 (jonnylin13) | https://github.com/jonnylin13/Maple83 |
| Maple_Pshark | https://github.com/obstriker/Maple_Pshark |
| MaplePacketPuller | https://github.com/Bratah123/MaplePacketPuller |
| MapleShark | https://github.com/diamondo25/MapleShark |
| mapleSniffer | https://github.com/icelemon1314/mapleSniffer |
| MaplePE | https://github.com/zhyonc/MaplePE |
| MapleRef Wiki Packets | https://mapleref.fandom.com/wiki/Packets |
| MapleRef Wiki Opcode | https://mapleref.fandom.com/wiki/Operation_Code |
| STREDIT | https://forum.ragezone.com/threads/release-stredit-localhost-editor.810762/ |
| Dynamic NPC Shop Script | https://forum.ragezone.com/threads/dynamic-npc-shop-script-v83.1089979/ |
| NPC TYPE Thread | https://forum.ragezone.com/threads/npc-type.1262188/ |
| Hex Edit Tutorial (maplesecrets) | http://maplesecrets.blogspot.com/2013/04/how-to-hex-edit-clients-easily.html |
| v83 Setup Video Guide | https://forum.ragezone.com/threads/video-guide-how-to-make-a-maplestory-v83-private-server-–-full-setup-tutorial.1243566/ |

---

## 下次研究重點

1. MapleStory v83 客戶端記憶體修改進階技巧
2. IDA Pro 7.x 配合 SpiritIDAPlugin 實際操作
3. MapleCrypto 加密實戰解密練習
4. WZ 編輯實戰：新增自訂 NPC 和地圖

---

*🐱 超級貓咪 - 2026-03-28 08:57 UTC (第七十次)*
