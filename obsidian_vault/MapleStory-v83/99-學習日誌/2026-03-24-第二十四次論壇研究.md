# 2026-03-24 第二十四次論壇研究

## 研究時間
2026-03-24 23:27 UTC

## 本次研究重點
1. 登入器/客戶端修改
2. 抓包分析
3. Opcode結構
4. WZ修改
5. NPC腳本編寫

---

## 📌 登入器/客戶端修改

### v83 客戶端完整架設流程 (RaGEZONE, April 2025)

**影片教學**: https://forum.ragezone.com/threads/video-guide-how-to-make-a-maplestory-v83-private-server-–-full-setup-tutorial.1243566/

**環境需求**:
- JDK 8 + JCE 8 (Java Cryptography Extension)
- MySQL Server 5.5
- v83 客戶端
- HxD Hex Editor（修改 IP）

**完整架設步驟摘要**:
```
1. 安裝 JDK 8
2. 安裝 MySQL Server 5.5
3. 下載伺服器源（HeavenMS/Cosmic/MapleSolaxiaV2）
4. 設定資料庫
5. 修改客戶端 IP（HxD → 127.0.0.1 或 server IP）
6. 啟動伺服器
7. 執行 localhost.exe 連接
```

### MapleSolaxiaV2 客戶端設定 (RaGEZONE, 2025)

**GitHub**: https://github.com/skhzhang/MapleSolaxiaV2

**下載資源**:
- 伺服器源: https://github.com/ronancpl/MapleSolaxiaV2
- 客戶端和工具: https://drive.google.com/drive/folders/0BzDsHSr-0V4MYVJ0TWIxd05hYUk

**流程**:
```
1. 執行 MapleGlobal-v83-setup.exe，安裝到選擇的目錄
2. 刪除 HShield 資料夾（整個）
3. 刪除 ASPLauncher.exe、MapleStory.exe、Patcher.exe
4. 覆蓋 WZ 檔案（用 client_wz 資料夾的內容）
5. 使用 localhost.exe 連接，若非本機需 HEX 編輯修改 IP
```

### Ezorsia v2 - Standalone HD DLL 客戶端 (RaGEZONE, Nov 2023)

**GitHub**: https://github.com/444Ro666/MapleEzorsia-v2

**特點**:
- 將官方 v83 客戶端改造為 HD + localhost 版本
- 無需刪除任何原廠檔案
- 自生成 EzorsiaV2_UI.wZ，無 WZ 衝突
- 支援最多 3 個自訂 DLL
- 修復 Cash Shop、登入畫面、解析度縮放

**使用方式**: 只需將 dinput8.dll 放入遊戲目錄即可

**客戶端設定 Wiki**: https://github.com/444Ro666/MapleEzorsia-v2/wiki/v83‐Client‐Setup‐and‐Development‐Guide

### localhost.exe IP 修改方法 (STREDIT)

**工具**: STREDIT v2.9.2.0 (RaGEZONE)

```
流程：
1. 開啟 STREDIT，載入 localhost.exe
2. 詢問是否為 v118+ → 選「No」
3. 詢問 IP 搜尋 → 選「Yes」
4. 點擊「Force to a IP」，輸入目標 IP
5. 儲存並關閉
```

**優點**: 自動處理 IP 位址長度問題

### v83 仍是私人伺服器主流版本 (Reddit, Feb 2025)

**評價**: "V83 is the most modded/documented online. Editing npcs/items/stores? Very easy, many guides on how to..."

**原因**:
- 版本成熟、文檔完整
- 工具鏈豐富
- 大量已有腳本和WZ資源

**來源**: https://www.reddit.com/r/mapleservers/comments/1isuicz/single_player_server_v83/

---

## 📌 抓包分析

### Maple_Pshark - v83 專用封包 Logger

**GitHub**: https://github.com/obstriker/Maple_Pshark

**功能**:
- 專為 MapleRoyals 等私人伺服器設計
- Hook 加密函數，直接截獲明文
- 支援攔截、修改、注入 Recv/Send 封包
- 可視化封包流量

### MaplePacketPuller + SpiritIDAPlugin 工作流程 (2026)

**GitHub**:
- MaplePacketPuller: https://github.com/Bratah123/MaplePacketPuller
- SpiritIDAPlugin: https://github.com/Bratah123/SpiritIDAPlugin

**支援環境**:
| 環境 | 支援狀態 |
|------|---------|
| Python 3.8.5 | ✅ |
| Python 3.6.12 / 3.8.6 | ✅ |
| IDA Pro 7.0 (32-bit) | ✅ |
| IDA Pro 7.5 | ✅ |
| IDA Pro 6.8 | ✅ |
| Python 2.7 | ❌ |

**完整工作流程**:
```
Step 1: IDA Pro 反編譯
  → 在 IDA 中找到 CField::OnPacket 或 CLogin::OnPacket
  → 產生 pseudocode

Step 2: 複製 pseudocode
  → 在 Functions/ 目錄建立 .txt 檔案
  → 貼上 IDA 產生的 C-style pseudocode

Step 3: Python 分析
  → cd MaplePacketPuller/IDA\ Maple\ Script/src/main/python
  → python main.py
  → 輸入分析類型

Step 4: 輸出結果
  → 封包結構自動寫入 FuncOutput/ 目錄
```

### MapleRef Wiki - 封包結構參考

**網址**: https://mapleref.fandom.com/wiki/Packets

**重點**:
- 完整的封包結構文檔
- 涵蓋各版本 Opcode 對應關係
- 可配合 Wireshark 使用

### v83 封包加密三層架構

```
┌─────────────────────────────────────────────────────┐
│ Layer 1: MapleCrypto (頭部混淆)                     │
│   - 4 bytes header，含混淆 opcode + length          │
│                                                        │
│ Layer 2: Shanda Encryption (對稱加密)                 │
│   - v83 仍使用，已於 v118+ 移除                      │
│                                                        │
│ Layer 3: AES-OFB (主加密)                            │
│   - 256-bit key, 4-byte IV                          │
│   - 版本決定的靜態金鑰                              │
└─────────────────────────────────────────────────────┘
```

### MaplePE - 32-bit Packet Editor (2025 新工具)

**GitHub**: https://github.com/zhyonc/MaplePE

**功能**:
- 專為 MapleStory 設計的 32-bit 封包編輯器
- 可解析封包結構並發送自訂封包到客戶端
- 支援過濾特定 Opcode
- 可從目標進程提取 InPacket 和 OutPacket 資訊

---

## 📌 Opcode 結構

### CClientSocket::ProcessPacket 封包分發流程

```
CClientSocket::ProcessPacket()
    ↓
根據 MapleCrypto header 解密
    ↓
提取 opcode (2 bytes, little endian)
    ↓
switch(opcode)
    ↓
CLogin::OnPacket()    // 登入畫面
    或
CField::OnPacket()    // 遊戲中
```

### MapleCrypto Header 加密/解密公式

**加密端（發送）**:
```java
public byte[] getPacketHeader(int opcode, int length, int iv) {
    int verify = (opcode ^ (iv & 0xFF)) + ((iv >> 8) & 0xFF) * 0x100;
    verify = (verify << 3) | (verify >>> 5);
    verify &= 0xFFFF;
    return new byte[] {
        (byte) (verify & 0xFF),
        (byte) ((verify >>> 8) & 0xFF),
        (byte) (length & 0xFF),
        (byte) ((length >>> 8) & 0xFF)
    };
}
```

**解密端（接收）**:
```java
public int retrieveOpcode(byte[] header, int iv) {
    int verify = (header[0] & 0xFF) | ((header[1] << 8) & 0xFF00);
    verify = (verify >>> 3) | ((verify << 5) & 0xFFFF);
    return (verify - ((iv >> 8) & 0xFF) * 0x100) ^ (iv & 0xFF);
}
```

### IDA Decode 函數對照表

| IDA 函數 | MaplePacketWriter 方法 | 讀取大小 |
|---------|----------------------|---------|
| `Decode1` | `mplew.write` | 1 byte |
| `Decode2` | `mplew.writeShort` | 2 bytes |
| `Decode4` | `mplew.writeInt` | 4 bytes |
| `DecodeStr` | `mplew.writeMapleAsciiString` | 可變 |
| `DecodeBuffer(x, 8)` | `mplew.writeLong` | 8 bytes |

### v83 常用 Opcode 速查表

**登入相關**:
| Opcode | 名稱 | 說明 |
|--------|------|------|
| 0x01 | LOGIN_PASSWORD | 登入請求 |
| 0x06 | SERVERLIST_REQUEST | 請求伺服器列表 |
| 0x07 | CHARLIST_REQUEST | 請求角色列表 |
| 0x08 | CHAR_SELECT | 選擇角色 |
| 0x0B | PLAYER_DROP | 進入遊戲 |

**遊戲相關**:
| Opcode | 名稱 | 說明 |
|--------|------|------|
| 0x14 | MOVE_PLAYER | 移動 |
| 0x28 | NPC_TALK | NPC 對話 |
| 0x6A | MOB_APPLY_CONTROL | 怪物生成+HP顯示 |

---

## 📌 WZ 修改

### HaRepacker-resurrected - WZ 編輯器

**官方網站**: https://lastbattle.github.io/Harepacker-resurrected/
**GitHub**: https://github.com/lastbattle/Harepacker-resurrected

**v83 WZ 加密格式**: 選擇 `GMS (old)` 或 `GMS`

### WZ 檔案功能對照表

| WZ檔案 | 功能 |
|--------|------|
| Base.wz | 武器、技能、音效等基礎數據 |
| Character.wz | 角色相關Sprite和裝備 |
| Effect.wz | 傷害數字、墓碑、升級特效等 |
| Etc.wz | 任務、buff、配方、技能資訊 |
| Item.wz | 所有物品圖片和數據 |
| Map.wz | 所有地圖視覺 |
| Mob.wz | 所有怪物數據 |
| Npc.wz | 所有NPC資訊 |
| Quest.wz | 所有任務資訊 |
| Skill.wz | 所有職業技能 |
| Sound.wz | 音效和BGM |
| String.wz | 伺服器端字串（不可編輯） |

### WZ 編輯實戰技巧 (maplesecrets.blogspot.com)

**Monster GodMode (Miss)**:
```
1. Open deMSwZ
2. Open Mob.wz
3. Deselect everything except for acc
4. Give acc a Value of 0
5. Process All, Save
```

**Monster Speed 126**:
```
1. Open HaRepacker → Mob.wz
2. Go to monster ID
3. Double-click ID → [+] info → speed
4. Change value to 126
5. Save
```

### WZ 導出為 XML (RaGEZONE, 2023)

**來源**: https://forum.ragezone.com/threads/community-wz-img-repack-for-v83.1221835/

**步驟**:
```
1. 用 HaRepacker 打開 WZ 檔案
2. 選擇要導出的節點
3. 點擊 Export → 選擇 XML (Private Server) 格式
4. 導出後可使用文字編輯器修改
5. 重新導入 WZ
```

**注意**:
- v83 WZ 加密格式需選擇 `GMS (old)`
- 不要直接刪除/重新導入節點，可能造成數據丟失
- 建議使用 HaRepacker UI 直接編輯數值

---

## 📌 NPC 腳本編寫

### 狀態機核心概念

```javascript
status = -1 → 初始調用 start()
    ↓
status = 0 → 第一次 sendNext/sendSimple
    ↓
status = 1 → 玩家回應後
    ↓
... 依此類推
```

### Mode 參數含義

| Mode 值 | 意義 |
|---------|------|
| 1 | 點擊"下一頁"或選項 |
| 0 | 點擊"返回" |
| -1 | 關閉對話框 (ESC/X) |

### NPC 對話框類型

| Type 值 | 對話框類型 | 使用方法 |
|---------|-----------|---------|
| 0 | 標準對話 | `sendNext`, `sendOk`, `sendPrev` |
| 1 | Yes/No | `sendYesNo` |
| 2 | 選項選單 | `sendSimple` |
| 3 | 數字輸入 | `sendGetNumber` |
| 12 | 接受/拒絕 | `sendAcceptDecline` |

### NPC 對話格式標籤

| 標籤 | 功能 |
|------|------|
| `#b` | 藍色文字 |
| `#r` | 紅色文字 |
| `#g` | 綠色文字 |
| `#l` | 選項標記 |
| `#i[itemid]` | 物品圖片 |
| `#h#` | 玩家名稱 |
| `#c[itemid]#` | 持有物品數量 |
| `#t[itemid]` | 物品名稱 |

### 腳本源相容性問題

| 問題 | 解決方案 |
|------|---------|
| `importPackage is not defined` | JDK 7+ 廢棄，移除該行 |
| `SavedLocationType is not defined` | 使用相容格式 |
| NPC 腳本不相容 | 使用普遍 `cm.` 函數 |

### 快速傳送 NPC 腳本源 (RaGEZONE, 2020)

```javascript
var status = 0;

function start() {
    cm.sendYesNo("嘿，你想傳送到 Ludibrium 嗎？");
}

function action(mode, type, selection) {
    status++;
    if (mode != 1) {
        if(mode == 0 && type != 1)
            status -= 2;
        else if(type == 1 || (mode == -1 && type != 1)) {
            if(mode == 0) {
                cm.sendOk("嗯... 看來你還有事要在這裡處理？");
                cm.dispose();
                return;
            }
        }
    }

    if (status == 1) {
        if (cm.haveItem(4310000))
            cm.sendNext("#b檢測到快速傳送票#k。\r\n看來你有這張票！");
        else
            cm.sendNext("當然可以！我帶你去 Ludibrium。");
    } else if (status == 2) {
        cm.warp(220000000);
        cm.dispose();
    }
}
```

---

## 📚 參考資源

### 開源 v83 模擬器

| 專案 | GitHub | 特色 |
|------|--------|------|
| Cosmic | P0nk/Cosmic | 最活躍，持續更新 |
| HeavenMS | ronancpl/HeavenMS | 經典，功能豐富 |
| MapleSolaxiaV2 | skhzhang/MapleSolaxiaV2 | 基於 Solaxia |
| Maple83 | jonnylin13/Maple83 | 早期開源版本 |

### 工具下載

| 工具 | 官方網站 |
|------|---------|
| HaRepacker-resurrected | lastbattle.github.io/Harepacker-resurrected |
| HxD Hex Editor | mh-nexus.de/en/hxd/ |
| Wireshark | wireshark.org/ |
| Maple_Pshark | github.com/obstriker/Maple_Pshark |
| MaplePE | github.com/zhyonc/MaplePE |

### 論壇

| 論壇 | 網址 |
|------|------|
| RaGEZONE MapleStory | forum.ragezone.com/f428/ |
| elitepvpers MapleStory | elitepvpers.com/forum/maplestory/ |
| MapleRef Wiki | mapleref.fandom.com |
| Reddit r/mapleservers | reddit.com/r/mapleservers/ |

---

*🐱 超级猫咪 - 更新於 2026-03-24 (第二十四次)*
